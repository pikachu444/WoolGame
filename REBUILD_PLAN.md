# 털실 게임 전면 개편

2026-09-10. 이 문서는 현재 사용자와의 논의를 구현에 연결한다. 이전 문서와 코드는 이번 개편의 요구사항으로 승계하지 않는다. 기존 결과는 비교와 실패 원인 확인에만 사용한다.

## 목표와 판단 기준

‘털실 마스터’에서 사용자가 원한 경험은 **보기 좋은 뜨개 캐릭터를, 방향 블록 퍼즐을 풀어 한 올씩 해체하고, 실을 감아 고양이를 구하는 것**이다. 그림을 닮게 만드는 일과 재미있는 게임을 만드는 일을 함께 해결해야 한다.

완성 기준은 기능 개수나 자동 검사 통과 수가 아니다. 사용자가 처음 보았을 때 털실 게임으로 알아보고, 설명 없이 첫 수를 이해하고, 선택한 블록과 풀리는 실의 인과관계를 느끼며, 위험·성공·재도전을 납득할 수 있어야 한다. 현재 구현은 이 기준을 검증하는 중이며 완성 판정을 받지 않았다.

## 전체 구조와 서로 의존하는 결정

| 영역 | 설계할 내용 | 다른 영역에 주는 제약 | 완료 증거 |
|---|---|---|---|
| 플레이 경험 | 관찰 → 방향 확인 → 블록 추출 → 실패에 도착 → 같은 색 실 감기 → 빈자리 확보 | 화면에서 원인과 결과를 추적할 수 있어야 함 | 처음부터 클리어까지 실제 실행 영상 |
| 시각 방향 | 작은 실코, 겹치는 V형 몸통, 귀여운 얼굴, 차분한 직물 배경, 선명한 화살표 | 작은 모바일 화면에서도 실루엣과 색을 구분 | 실제 게임 캡처와 디자인 목표 비교 |
| 화면 구성 | 상단 캐릭터, 중간 실패 네 개, 하단 밀집 퍼즐, 최소한의 조작 버튼 | 몸통·실·블록의 이동이 정보와 버튼을 가리지 않음 | 시작·진행·위험·성공 상태의 화면 |
| 게임 규칙 | 방향 장애물, 이동 중 자리 예약, 보이는 같은 색만 수집, 정원, 완료·피해 조건 | 아트에 보이는 상태와 판정이 일치 | 양성·음성 규칙 검사와 플레이 확인 |
| 움직임 | 경로를 따르는 머리와 몸통, 실의 출발점, 풀림과 감김, 블록 이동 경로 | 눈앞에서 위치가 끊기거나 다른 블록을 통과하면 안 됨 | 느린 속도와 정상 속도로 연속 동작 검토 |
| 레벨 설계 | 방향 의존성과 색 대기칸 압박을 함께 설계, 학습에서 판단으로 난이도 상승 | 방향만 풀리는 배치도 색 순서가 막히면 실패 | 해법 기록, 잘못된 선택과 회복 가능성 확인 |
| 조작과 접근성 | 탭·드래그 구분, 일시정지, 재시작, 힌트, 소리, 색과 방향의 가독성 | 게임이 정지한 동안 판정과 애니메이션 모두 정지 | 입력 경계 검사, 상태 전환 확인 |
| 제작 구조 | 규칙 상태와 표현 분리, 수정 가능한 경로·배치, 일관된 아트 자산 | 미술 수정이 판정 변경으로 번지지 않음 | 코드 구조, 장면 파일, 빌드 재현 절차 |
| 실행 품질 | 연속 프레임, 해상도 대응, 앱 전환, 자산 누락 방지 | 고사양 PC 캡처만으로 모바일 품질 판정 금지 | 실행 로그·프레임 측정·Android 패키지 검사 |
| 전달 | 실행 파일, 원본 프로젝트, 실제 증거, 알려진 한계 | 시안과 실제 플레이를 혼동시키지 않음 | 직접 열 수 있는 결과물과 검증 기록 |

## 제작 방식

고정 시점 2.5D를 첫 구현 방식으로 검증한다. 캐릭터와 뜨개 질감은 전용 아트 자산으로 만들고, 경로·실·감김·블록 이동은 게임 상태로 움직인다. Godot/GDScript를 사용하되 엔진 교체 자체를 개선의 근거로 삼지 않는다. 이 방식으로 겹침, 방향, 질감, 움직임을 만족시키지 못하면 해당 표현을 다시 설계한다.

게임 전체를 한 번에 확장하지 않고, 전체 설계를 대표하는 한 레벨에서 품질을 먼저 입증한다. 그동안 다른 영역을 잊지 않도록 위 표를 유지한다. 대표 레벨이 보기에도 플레이하기에도 성립한 뒤 레벨 변화와 진행 흐름을 연결한다. 임시 그림이나 장식만으로 표현 결함을 덮지 않는다.

## 검토 지점과 되돌아갈 곳

1. **경험과 시각 목표**: 참고 화면에서 무엇을 재현해야 하는지 명확한가? 아니다 → 구현량을 늘리지 않고 구도·실루엣·행동을 다시 정한다.
2. **대표 플레이 장면**: 눌렀을 때 블록 이동, 수집, 풀림, 감김이 하나의 행동으로 보이는가? 아니다 → 해당 애니메이션과 판정을 함께 수정한다.
3. **레벨과 전체 흐름**: 색과 방향이 실제 판단을 요구하는가? 성공·실패·재시작이 끝까지 이어지는가? 아니다 → 배치·속도·정원·피드백을 다시 설계한다.
4. **실제 환경**: 전체 플레이 중 누락, 끊김, 잘못된 입력, 화면 침범이 없는가? 아니다 → 원인을 분리해 고친 뒤 영향받은 지점을 재검토한다.
5. **전달 준비**: 제출한 파일이 검증한 실행과 같은가? 안 해 본 검증을 완료라고 적지 않았는가? 통과한 범위와 남은 한계를 기록한다.

한 검토 지점의 실패를 다른 검사의 성공으로 상쇄하지 않는다. 규칙 검사를 통과해도 화면이 나쁘면 시각 작업은 미완료다. 시안이 좋아도 실제 플레이가 불안정하면 실행 작업은 미완료다.

## 원본 대조에서 폐기하고 수정한 결정

이전 720×1560 구도, 크림색 하단 바닥, 세로 목재 실패, 커다란 6×7 보드는 사용자의 지적과 독립 비교에서 미달로 판정했다. 이 선택을 현재 설계의 근거로 유지하지 않는다.

V04 원본 동영상의 게임 영역 592×1138을 직접 측정했다. 무대 420px, 실패 작업대 133px, 블록 영역 485px, 도구 100px를 사용하며, 광고·OS 영역은 게임 화면 밖으로 제외한다. 영상 첫 장면은 76% 진행 상태이므로 남아 있는 9블록을 그대로 배치하고, 감기 중인 실패 3개와 진행률을 함께 복원했다.

몸통이 82%부터 사라지는 문제, 납작한 감김, 크림색 완료 모달도 독립 리뷰에서 미달로 판정되어 수정했다. 진행률별 비교와 최종 실행 영상, 남은 이미지·동작 차이는 Evidence/Rebuild/ReferenceMatch에 기록한다. 기능 검사 성공으로 시각 미달을 상쇄하지 않는다.

## 사용자 플레이에서 계속 확인할 점

첫 수가 자연스럽게 이해되는지, 실을 반복해서 감는 동작이 만족스러운지, 방향 퍼즐과 색 대기의 판단이 충분한지 확인해야 한다. 지금의 세 레벨이 장기적인 난이도 구성을 입증하지는 않는다. 실제 휴대폰에서 손가락 조작, 화면 가독성, 프레임과 발열도 확인할 필요가 있다.

검사 통과를 사용자 만족의 대리 지표로 삼지 않는다. 다음 수정의 우선순위는 실제 플레이에서 생기는 답답함과 어색함을 기준으로 잡는다.
